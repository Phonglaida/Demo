import React from 'react';
import { compose, setDisplayName } from 'recompose';
import { withStepper } from './with-stepper';
/**
 * A component that display the title of the active page.
 *
 * ### Usage
 * ```
 * <StepTitle />
 * ```
 */
export var StepTitle = compose(withStepper, setDisplayName('StepTitle'))(function (_ref) {
  var pages = _ref.pages,
      currentPage = _ref.currentPage,
      style = _ref.style;
  return React.createElement("span", { style: style }, pages[currentPage].title);
});
//# sourceMappingURL=step-title.js.map
