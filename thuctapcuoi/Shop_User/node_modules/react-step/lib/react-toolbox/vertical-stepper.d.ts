/// <reference types="react" />
import { StatelessComponent } from 'react';
import { Context } from '../entities';
export declare type Props = {
    activeColor?: string;
    alertColor?: string;
};
export declare const StatelessVerticalStepper: ({pages, currentPage, setPageIndex, activeColor, alertColor}: Props & Context) => JSX.Element;
/**
 * A component that renders the mobile version of the vertical stepper
 * from the [material design guidlines](https://material.io/guidelines/components/steppers.html#steppers-types-of-steppers)
 */
export declare const VerticalStepper: StatelessComponent<Props>;
