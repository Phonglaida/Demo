/// <reference types="react" />
import { CSSProperties } from 'react';
import { Status } from '../entities';
export declare type Props = {
    step: number;
    active?: boolean;
    status?: Status;
    activeColor?: string;
    alertColor?: string;
    style?: CSSProperties;
};
/**
 * An material warning icon to use for steps that needs the
 * users attention.
 */
export declare const AlertIcon: ({alertColor}: {
    alertColor?: string;
}) => JSX.Element;
/**
 * A material check icon in a circle to use for steps that
 * are completed.
 */
export declare const DoneIcon: ({activeColor}: {
    activeColor?: string;
}) => JSX.Element;
/**
 * A circle with the step number to use for most steps.
 */
export declare const StepNumber: ({step, active, activeColor}: {
    step: any;
    active: any;
    activeColor?: string;
}) => JSX.Element;
/**
 * Renders the appropriate step icon for the current status.
 */
export declare const StepCircle: ({step, active, status, style, activeColor, alertColor}: Props) => JSX.Element;
