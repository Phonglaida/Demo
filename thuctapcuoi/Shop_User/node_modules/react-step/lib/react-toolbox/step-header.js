import React from 'react';
import { Column } from 'styled-material/cjs/layout';
import { ClickableRow, defaultAlertColor } from '../utils';
import { StepCircle } from './step-circle';
/**
 * A step header after [material design](https://material.io/guidelines/components/steppers.html#steppers-specs)
 * renders a step icon and title. Used for building material design based steppers.
 */
export var StepHeader = function StepHeader(_ref) {
    var step = _ref.step,
        active = _ref.active,
        page = _ref.page,
        activeColor = _ref.activeColor,
        _ref$alertColor = _ref.alertColor,
        alertColor = _ref$alertColor === undefined ? defaultAlertColor : _ref$alertColor,
        style = _ref.style,
        onClick = _ref.onClick;
    return React.createElement(ClickableRow, { disabled: active, horizontal: "center", style: Object.assign({ paddingBottom: 16 }, style), onClick: onClick }, React.createElement(StepCircle, { step: step, active: active, status: page.status, activeColor: activeColor, alertColor: alertColor }), React.createElement(Column, { vertical: "center" }, React.createElement("span", { style: {
            paddingLeft: 4,
            color: page.status === 'alert' ? alertColor : active ? 'rgba(0, 0, 0, 0.87)' : 'rgba(0, 0, 0, 0.38)',
            fontSize: 14,
            fontWeight: active ? 500 : 400
        } }, page.title)));
};
//# sourceMappingURL=step-header.js.map
