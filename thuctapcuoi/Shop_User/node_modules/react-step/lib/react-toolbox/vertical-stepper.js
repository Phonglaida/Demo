import React from 'react';
import { VerticalLine } from '../utils';
import { withStepper } from '../with-stepper';
import { StepHeader } from './step-header';
var enhance = withStepper;
export var StatelessVerticalStepper = function StatelessVerticalStepper(_ref) {
    var pages = _ref.pages,
        currentPage = _ref.currentPage,
        setPageIndex = _ref.setPageIndex,
        activeColor = _ref.activeColor,
        alertColor = _ref.alertColor;
    return React.createElement("div", { style: { position: 'relative', marginTop: 24 } }, React.createElement(VerticalLine, null), React.createElement("div", { style: { paddingLeft: 60 } }, pages.map(function (page, i) {
        return React.createElement("div", { key: i, style: currentPage === i - 1 ? { paddingTop: 32 } : {} }, React.createElement(StepHeader, { step: i + 1, active: currentPage === i, page: page, onClick: function onClick() {
                return setPageIndex(i);
            }, style: { position: 'relative', right: 44 }, activeColor: activeColor, alertColor: alertColor }), currentPage === i && (page.render ? page.render() : React.createElement(page.component, null)));
    })));
};
/**
 * A component that renders the mobile version of the vertical stepper
 * from the [material design guidlines](https://material.io/guidelines/components/steppers.html#steppers-types-of-steppers)
 */
export var VerticalStepper = enhance(StatelessVerticalStepper);
//# sourceMappingURL=vertical-stepper.js.map
