/// <reference types="react" />
import { CSSProperties, StatelessComponent } from 'react';
/**
 * A component for tracking the current and total pages
 * using dots.
 *
 * ### Usage
 * ```
 * <DotTracker />
 * ```
 */
export declare const DotTracker: StatelessComponent<{
    activeColor?: string;
    inactiveColor?: string;
    style?: CSSProperties;
}>;
