/// <reference types="react" />
import { CSSProperties, StatelessComponent } from 'react';
/**
 * A component that display the title of the active page.
 *
 * ### Usage
 * ```
 * <StepTitle />
 * ```
 */
export declare const StepTitle: StatelessComponent<{
    style?: CSSProperties;
}>;
